# File to launch the program on the ingi server.


Executable   = ../srcc$(process)/main.py

Log          = ingi_logs.log

Output       = ingi_logs.outputfile.$(Process)

requirements = Arch == "X86_64"

Queue 73
